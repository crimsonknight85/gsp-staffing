# ── Public (exposed to browser, safe to share) ────────────
NEXT_PUBLIC_SUPABASE_URL=https://deymcvhrblfbmdgvnytq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_BagxhcEvLixgmLYflhGmQw_mTsQiQjX

# ── Server-only secret (NEVER expose to browser) ──────────
# Get this from Supabase Dashboard → Settings → API → service_role key
# Add it to Vercel as an environment variable — do NOT commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
